package com.aa.mynotes.ui.theme

import androidx.compose.ui.graphics.Color

// Mirrors the values actually used from res/values/colors.xml (the "Green/Red" palette).
// Status bar tinting still comes from the XML AppTheme (colorPrimaryDark), untouched by Compose.
val ColorPrimary = Color(0xFF4CAF50)
val ColorAccent = Color(0xFFF44336)
val ColorPrimaryText = Color(0xFF212121)
